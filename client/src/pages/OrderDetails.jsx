import{useEffect,useState}from'react';import{useParams,useNavigate,Link}from'react-router-dom';import{API}from'../api/http.js';import OrderTimeline from'../components/OrderTimeline.jsx';export default function OrderDetails(){const{id}=useParams(),nav=useNavigate(),[d,setD]=useState(null),[busy,setBusy]=useState(false);useEffect(()=>{API.get('/orders/'+id).then(r=>setD(r.data.data))},[id]);if(!d)return <main className="center"><div className="spinner"/></main>;const{o,order,history}=d;const x=order||o;const cancel=async()=>{setBusy(true);try{await API.post('/orders/'+id+'/cancel');nav('/orders')}catch(e){alert(e.response?.data?.message||'Cannot cancel')}finally{setBusy(false)}};return <main className="orderDetails"><div className="pageIntro"><span className="eyebrow">ORDER</span><h1>{x.orderNumber}</h1><p>{new Date(x.createdAt).toLocaleString('en-IN')}</p></div><OrderTimeline status={x.orderStatus}/><div className="detailGrid"><section className="panel"><h3>Items</h3>{x.items.map(i=><div className="lineItem" key={i.sku}><img src={i.image||'/products/placeholder.svg'} alt=""/><span>{i.name}<small>Qty {i.quantity}</small></span><b>₹{(i.price*i.quantity).toFixed(2)}</b></div>)}</section><section className="panel"><h3>Delivery</h3><p>{Object.values(x.shippingAddress||{}).filter(v=>typeof v==='string'&&v).join(', ')}</p>{x.trackingNumber&&<p><b>Tracking:</b> {x.trackingNumber}</p>}</section><section className="panel"><h3>Payment & total</h3><div className="grand"><span>{x.paymentMethod}</span><b>₹{x.grandTotal.toFixed(2)}</b></div></section><section className="panel"><h3>Actions</h3>{['PENDING','CONFIRMED','PROCESSING'].includes(x.orderStatus)&&<button disabled={busy} className="dangerBtn" onClick={cancel}>Cancel order</button>}{['SHIPPED','OUT_FOR_DELIVERY'].includes(x.orderStatus)&&<p className="muted">Cancellation is disabled after shipment.</p>}{x.orderStatus==='DELIVERED'&&<Link className="secondary" to={'/orders/'+id+'/return'}>Request return</Link>}</section></div></main>}