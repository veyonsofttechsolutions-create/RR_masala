import {Router} from 'express'; import * as c from '../controllers/userController.js'; import {auth,roles} from '../middleware/auth.js'; const r=Router(); r.use(auth); r.patch('/profile',roles('CUSTOMER','ADMIN'),c.profile); r.patch('/password',c.password); r.put('/addresses',c.addresses); r.get('/admin/all',roles('ADMIN','STAFF'),c.adminList); r.patch('/admin/:id/toggle',roles('ADMIN'),c.toggle); export default r;