import {Router} from 'express'; import * as c from '../controllers/productController.js'; import {auth,roles} from '../middleware/auth.js'; const r=Router(); r.get('/',c.list); r.get('/admin/all',auth,roles('ADMIN','STAFF'),c.adminList); r.get('/:slug',c.get); r.post('/',auth,roles('ADMIN','STAFF'),c.create); r.put('/:id',auth,roles('ADMIN','STAFF'),c.update); r.delete('/:id',auth,roles('ADMIN'),c.remove); export default r;